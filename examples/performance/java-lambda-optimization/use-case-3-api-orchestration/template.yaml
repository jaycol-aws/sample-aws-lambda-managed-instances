AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  LMI Blog — Use Case 3: API Orchestration (Standard Lambda)
  I/O + concurrency workload — connection pooling and thread utilization.

Parameters:
  MemorySize:
    Type: Number
    Default: 2048
    AllowedValues: [512, 1024, 2048, 3008]
    Description: Lambda memory in MB.

Globals:
  Function:
    Tracing: Active
    CodeUri: target/api-orchestration-1.0.0-SNAPSHOT.jar
    Handler: org.springframework.cloud.function.adapter.aws.FunctionInvoker::handleRequest
    Runtime: java21
    Timeout: 30
    MemorySize: !Ref MemorySize
    Architectures: [x86_64]
    Environment:
      Variables:
        ORDER_TABLE_NAME: !Ref OrderTable
        PRODUCT_TABLE_NAME: !Ref ProductTable
        NOTIFICATION_QUEUE_URL: !Ref NotificationQueue
        ALERT_TOPIC_ARN: !Ref AlertTopic
        MAIN_CLASS: com.aws.lmi.api.ApiOrchestrationApplication
        JAVA_TOOL_OPTIONS: "-XX:+UseG1GC -Xlog:gc:stderr:time,tags"

Resources:
  OrderApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: prod
      Name: lmi-blog-api-standard

  ProcessOrderFunction:
    Type: AWS::Serverless::Function
    Properties:
      Environment:
        Variables:
          SPRING_CLOUD_FUNCTION_DEFINITION: orderProcessorFunction
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref OrderTable
        - DynamoDBCrudPolicy:
            TableName: !Ref ProductTable
        - SQSSendMessagePolicy:
            QueueName: !GetAtt NotificationQueue.QueueName
        - SNSPublishMessagePolicy:
            TopicName: !GetAtt AlertTopic.TopicName
      Events:
        PostOrder:
          Type: Api
          Properties:
            RestApiId: !Ref OrderApi
            Path: /orders
            Method: post

  ProcessOrderFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${ProcessOrderFunction}"
      RetentionInDays: 7

  OrderTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${AWS::StackName}-orders"
      AttributeDefinitions:
        - AttributeName: customerId
          AttributeType: S
        - AttributeName: orderId
          AttributeType: S
      KeySchema:
        - AttributeName: customerId
          KeyType: HASH
        - AttributeName: orderId
          KeyType: RANGE
      BillingMode: PAY_PER_REQUEST

  ProductTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${AWS::StackName}-products"
      AttributeDefinitions:
        - AttributeName: productId
          AttributeType: S
      KeySchema:
        - AttributeName: productId
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST

  NotificationQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub "${AWS::StackName}-notifications"

  AlertTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: !Sub "${AWS::StackName}-alerts"

Outputs:
  ApiEndpoint:
    Value: !Sub "https://${OrderApi}.execute-api.${AWS::Region}.amazonaws.com/prod/orders"
  OrderTableName:
    Value: !Ref OrderTable
  ProductTableName:
    Value: !Ref ProductTable
